if(incomplete) {
  if(message) {
    std:ui:notice(message)
  } else {
    std:ui:notice("Please complete all the required fields.")
  }
}

if(additionalUI.formTop) {
  render(additionalUI.formTop)
}

std:form(form)

if(additionalUI.formBottom) {
  render(additionalUI.formBottom)
}