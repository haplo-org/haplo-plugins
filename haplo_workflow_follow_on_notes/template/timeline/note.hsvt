i("{} added a{private} follow-on note") { <i> entry.user.name </i> } private{
    if(entry.data.private) { " " <b> i("NAME(std:workflow:notes-private-adjective|private)") </b> }
}
<div class=[
    "z__workflow_timeline_entry_note"
    if(entry.data.private){"z__workflow_timeline_entry_note_private"}
  ]>
</div>