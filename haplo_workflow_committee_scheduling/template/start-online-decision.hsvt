std:plugin:resources("start-online-decision.js")

pageTitle(i("Start online decision process"))

<p><button class="select-all">i("Select all committee members")</button> <button class="deselect-all" style="margin-left:20px;">i("Select none")</button></p>

<form method="POST"> std:form:token()
  std:form(instance)
  <p><input type="submit" value=i("Send Invitation to Selected Committee Members")></p>
</form>
