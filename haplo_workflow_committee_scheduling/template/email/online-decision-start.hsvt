#option:no-tag-attribute-quote-minimisation

<h1>title</h1>

<p>i("You are invited to participate in the NAME(online decision) process for {}") { <a href=appUrl>appTitle</a> }</p>

if(note) {
    <p>i("Note: {}") { note }</p>
}
if(deadline) {
    <p>i("Please respond by: {}") { std:date(deadline) }</p>
}

<p class="button"> <a href=appUrl>appTitle</a> </p>

