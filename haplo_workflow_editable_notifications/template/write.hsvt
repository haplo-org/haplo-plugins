pageTitle(M.title ": " displayName)
backLink(M.url) { i("Cancel") }

<form method="POST"> std:form:token()
  std:form(form)
  <p><input type="submit" value=i("Review notification")> "&nbsp; &nbsp;" <input type="submit" value=i("Save for later") name="save"></p>
</form>
