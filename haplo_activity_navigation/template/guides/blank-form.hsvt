pageTitle(form.title)
backLink("/do/activity/" activity.name)
std:plugin:resources("blank-form.js")

std:ui:notice("This is an example form for information only.")

<form method="POST" id="blank-form" class="oforms-no-autofocus"> std:form:token()
    each(forms) {
        <h1> form.formTitle </h1>
        std:form(instance)
    }
</form>
